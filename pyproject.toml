[project]
name = "agentmesh"
version = "0.0.1"
description = "AgentMesh agent orchestration framework"
authors = [{name="You", email="you@example.com"}]
readme = "README.md"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
